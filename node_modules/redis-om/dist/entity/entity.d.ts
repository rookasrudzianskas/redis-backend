import { Point } from "../schema/schema-definitions";
import Schema from "../schema/schema";
export declare type EntityValue = number | boolean | string | Point | Date | string[];
export declare type EntityData = Record<string, EntityValue>;
export declare type EntityConstructor<TEntity> = new (schema: Schema<any>, id: string, data?: EntityData) => TEntity;
export default abstract class Entity {
    readonly entityId: string;
    readonly entityData: EntityData;
    private schemaDef;
    private prefix;
    constructor(schema: Schema<any>, id: string, data?: EntityData);
    get keyName(): string;
    toJSON(): Record<string, any>;
}
