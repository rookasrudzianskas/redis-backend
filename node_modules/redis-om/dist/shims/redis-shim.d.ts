import { createClient } from 'redis';
export declare type RedisConnection = ReturnType<typeof createClient>;
export default class RedisShim {
    private redis;
    constructor(urlOrConnection: string | RedisConnection);
    open(): Promise<void>;
    close(): Promise<void>;
    execute<TResult>(command: string[]): Promise<TResult>;
    unlink(key: string): Promise<void>;
    expire(key: string, ttl: number): Promise<void>;
    get(key: string): Promise<string | null>;
    set(key: string, value: string): Promise<string | null>;
    hgetall(key: string): Promise<{
        [x: string]: string;
    }>;
    hsetall(key: string, data: {
        [key: string]: string;
    }): Promise<void>;
}
